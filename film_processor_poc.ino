/**
 * Minimal version of film development processor.
 *
 * This project is more a proof of concept than something else, but work pretty well.
 *
 * I don't like Arduino as platform, but used it to made project easy to follow and reproduce.
 *
 * Firmware based on FreeRTOS 10.4.4-1.
 *
 * There is no optimizations of any sort, because it POC.
 */
